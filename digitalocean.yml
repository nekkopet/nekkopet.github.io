name: dvidsilva-astro
build:
  image: node:lts
  commands:
    - npm install
    - npm run build
run:
  image: nginx:alpine
  entrypoint: /bin/sh
  command:
    - -c
    - |
      cp -r /workspace/dist/* /usr/share/nginx/html/
      exec nginx -g 'daemon off;'